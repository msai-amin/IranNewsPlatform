# API Configuration
# Copy to .env.local and ensure this matches your running API (e.g. http://localhost:8000).
# If the main UI shows old or no data, hard refresh (Cmd+Shift+R / Ctrl+Shift+R) and check API: curl http://localhost:8000/api/health
NEXT_PUBLIC_API_URL=http://localhost:8000
